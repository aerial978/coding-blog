<header>
    <h1>My blog</h1>
    <nav>
        <a href="{{ base_path }}">Home page</a>

        {% if is_authenticated %}
            <a href="{{ base_path }}account">Mon compte</a>

            <form method="post" action="{{ base_path }}logout" style="display:inline;">
                <input type="hidden" name="csrf_token" value="{{ logout_csrf_token }}">
                <button type="submit">Déconnexion</button>
            </form>
        {% else %}
            <a href="{{ base_path }}login">Login</a>
            <a href="{{ base_path }}register">Register</a>
        {% endif %}
    </nav>
</header>